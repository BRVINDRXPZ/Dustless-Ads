# Lead Routing

> **Planning specification — inactive.** No live accounts, credentials, communications, or real customer data.

## Purpose

Deterministic assignment proposal pending owner approval.

## Rules

Validate spam/duplicate → identify segment → check service/geography without discriminatory proxies → assign by approved role and availability → create human-response task → preserve original source. High-value is not defined until owner sets criteria.

## Fallback

If owner unavailable or field missing, assign shared untriaged queue; never silently drop. Escalation hours, Northshore rules, service areas and response hours are owner decisions.

## Audit

Record rule version, matched condition, assignee, timestamp, overrides and reason. No customer message is activated.
