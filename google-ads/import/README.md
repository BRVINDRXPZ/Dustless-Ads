# Google Ads Import Workspace

These Phase 4 CSVs are reviewed, normalized **offline specifications**, not a promise of one-click compatibility with the current Google Ads Editor. Current headers, enum values, asset operations, location IDs, conversion creation/import workflow, and character/policy limits must be mapped and previewed in the installed current Editor before use. All entities are paused. Nothing here authorizes account connection, upload, publication, live changes, or spend.

`Launch status` expresses the strategic gate; platform status remains paused. Replace and QA every placeholder URL/value, resolve every owner/proof gate, run `../LAUNCH_CHECKLIST.md`, obtain explicit approval, and retain the import preview plus rollback plan.
